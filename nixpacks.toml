[phases.setup]
aptPkgs = [
  "php8.2",
  "php8.2-common",
  "php8.2-mysql",
  "php8.2-mbstring",
  "php8.2-xml",
  "php8.2-zip",
  "php8.2-curl",
  "php8.2-gd"
]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"
